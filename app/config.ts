/**
 * `PROFILE_CACHE_EXPIRY_DURATION_MS` is the duration in miliseconds a profile can remain in local storage without us fetching a new one
 *  The default value of `600000` is equivalent to 10 minutes.
 */
export const PROFILE_CACHE_EXPIRY_DURATION_MS = 600_000;
