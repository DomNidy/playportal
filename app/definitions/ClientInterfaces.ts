/**
 * This interface defines the data that should be assosciated with a connection component (ex. `<SpotifyConnection/>`)
 */
interface ConnectedAccountData {
  email?: string;
  profilePicURL?: string;
  profileURL?: string;
}
